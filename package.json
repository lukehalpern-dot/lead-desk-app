{
  "name": "lead-desk-app",
  "version": "1.0.0",
  "description": "The Lead Desk - a shared job-lead tracking tool, now a real hosted app instead of a Claude artifact.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5",
    "@anthropic-ai/sdk": "^0.32.1"
  }
}
